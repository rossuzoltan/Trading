"""
Legacy placeholder retained to avoid a broken import path.

The old example referenced RecurrentPPO and helper functions that no longer
exist in the current MaskablePPO plus volume-bar pipeline.

Use live_bridge.py for the real live or simulation bridge, or run
project_healthcheck.py first to verify that the required model, scaler, and
environment files exist.
"""

raise SystemExit(
    "\n"
    "live_bridge_example.py has been retired.\n"
    "Use live_bridge.py for the current event-driven bridge.\n"
    "Run project_healthcheck.py first if you want a quick repo audit.\n"
)
